# Project notes

## Owner preferences (Menu Sadah / bero)

- **Mission Control for every build**: for any substantive build task, publish a "Mission Control"
  Artifact FIRST (status board: phases, what each agent is doing, ETAs, overall %), give the user
  the link, and redeploy it at every milestone. The existing board lives at
  https://claude.ai/code/artifact/49b80672-1437-4f86-a279-7604873c6d7a — reuse/update it via the
  Artifact tool's `url` parameter rather than minting new links, unless the build is a totally new mission.
- Communication style: user prefers short, ADHD-friendly, visually scannable updates (tables, emoji,
  bold numbers). Lead with what's done and what they must do.
- Quality bar: user expects multi-agent expert swarms ("psychologists, Gulf cafe marketing experts,
  software pros, subscription pros") plus QA/"medics" verification agents on important builds.
- Menu Sadah brand: cream/parchment ground, gold + terracotta accents, serif display, bilingual
  Arabic/English (natural Gulf Arabic, never literal translation). Audience: independent Gulf cafe
  owners on iPhone, arriving from WhatsApp/Instagram.
- Never invent business data (cafes, handles, numbers) — every lead must trace to real sources.
- The user's ClientOS app is a launcher-only .app; its real data is `~/Desktop/ClientOS/index.html`
  on their Mac. The menu-sadah website source lives on their Mac at
  `~/Desktop/04 🍽 MENU SADAH HQ/MENU-SADAH-MOVE/menu-sadah` (not on GitHub; menu-sadah.com blocks fetches).
