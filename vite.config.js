import { defineConfig } from 'vite'

// The app is a single self-contained static file (index.html). No framework,
// no build step required — Vite is here only as a convenient dev server.
// https://vite.dev/config/
export default defineConfig({})
