# MENU SADAH · MAGIC MENU BUILD BRIEF v2 (maxed)

Paste this per cafe (or per batch). Fields in [brackets] are the only thing you edit.

```
MENU SADAH · MAGIC MENU BUILD BRIEF v2
Cafe: [name] ([arabic name if known])
Instagram: @[handle]   City/Area: [e.g. Adliya, Bahrain]   Reach: [~followers]
Known links: [talabat/website/menu-platform/linktree if any]
Inspiration build: [optional: link or repo path to a build I loved]
Fuel mode: [eco | standard | max]   Tier: [auto | A-full | B-teaser]
====================================
Build a premium bilingual (AR+EN) Menu Sadah menu to the locked standard in CLAUDE.md.

0. DATA GATE first (never skip, never fake): find their real menu
   (platform API, Talabat/delivery listing, published PDF, posted menu photos).
   - Real data extractable → Tier A: full 1:1 menu, OCD fidelity audit.
   - Not extractable → Tier B teaser: their brand fully alive + 8-15 items
     verified from public posts/listings + "full menu goes live within 24h
     of your yes". NEVER invent items or prices. Label nothing as complete
     that isn't.
1. SPY the brand: logo, colors (sample hex from their real assets), exact
   fonts (closest self-hosted equivalents), their own item artwork, address,
   WhatsApp, VAT no., reservation/delivery links, their caption voice.
2. SIGNATURE HERO: their own logo mark rebuilt as animated SVG under the
   wordmark. Elegant, abstract, never childish. Entrance stagger + infinite
   subtle breathe + reduced-motion fallback. PLUS one signature
   micro-interaction unique to this brand (the "nobody asked for this"
   detail people screenshot).
3. TYPE: EN slightly bigger than AR everywhere. Self-hosted woff2 only.
4. CURRENCY/LOCALE: use their real market format (Bahrain: BHD with 3
   decimals e.g. 2.500, VAT 10% note if shown on source; Saudi: SR integer).
   Copy their AR spellings exactly as they write them.
5. TURNKEY (all of it, always): live link menu-sadah.com/[slug] · verified
   QR pack + print.html + table-cards.pdf · HANDOFF.md (owner Arabic) ·
   OUTREACH.md (gift-first, voice-matched, no prices till asked, never say
   "QR menu", written like a person on a phone, no dashes) · BRAND-SPY.md ·
   add the lead to tracker/leads.js with status "built".
6. BULLETPROOF: 5x design passes at 375px in real Chromium, fidelity audit
   DOM vs source, no overflow, fonts loaded, QR decode sweep.
7. OFFER (only when asked): SR499 build + SR199/mo · founding SR999
   (SR500 deposit). Bahrain: BD 49 build + BD 19/mo · founding BD 99
   (BD 50 deposit).
Deliver, then report: link, tier, what makes this one special, and the
ready-to-send gift message.
```

## Batch mode (swarm)
For N cafes at once, add:

```
BATCH: run discovery first if I gave you a market instead of names
(rank by: brand beauty × menu weakness × data extractability × reach).
Build in QC batches of 6-8, flagship-quality first. Tier A before Tier B.
Every build passes the same gates as a solo build. Stop and show me the
pipeline before burning fuel on builds if feasibility looks thin.
```

## Fuel modes
- **eco**: Tier B teasers only, 3 design passes, batch QC via one collage screenshot per 8 builds.
- **standard**: mixed tiers, full gates.
- **max**: everything + one extra innovation pass per menu (custom easter egg, seasonal variant, dark-mode).
